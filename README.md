# DeepLearning_Numpy
A simple Linear regression model using Numpy.

Model mimics a simple neural network, one input one output:    y = mx + b.

Data is randomly generated with 100 data points used for training.  We then 
loop through a thousand epochs to train the model and arrive at values for parameters 
that comes closest to our original values.
