# DeepLearning_Numpy
A simple Linear regression model using Numpy.

Model mimics a simple neural network, one input one output:    y = mx + b.

Data is randomly generated with 1000 data points used for training.
